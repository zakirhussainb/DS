package com.zakcorp.leetcodemaster.contest;

import java.util.*;

public class Problem_C {
    static class Solver {
        public int solve1(int[] cookies, int k) {
//            int n = cookies.length;
//            Arrays.sort(cookies);
//            PriorityQueue<Integer> minHeap = new PriorityQueue<>();
//            int i = n - 1;
//            while(k --> 0) {
//                minHeap.add(cookies[i]);
//                i--;
//            }
//            for (int cookie : cookies) {
//                maxHeap.add(cookie);
//            }
//            for(int i = n - 1; i >= 0 && !maxHeap.isEmpty(); i--) {
//                int z = cookies[i] + maxHeap.poll();
//                maxHeap.add(z);
//            }
//            int ans = 0;
//            while(!maxHeap.isEmpty()) {
//                ans = Math.max(ans, maxHeap.poll());
//            }
//            return ans;
            return 0;2299
        }
    }
}
