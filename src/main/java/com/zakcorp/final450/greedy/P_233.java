package com.zakcorp.final450.greedy;

public class P_233 {
    /* sub-array vs sub-sequence vs subset
    sub-array :- contiguous sequence in an array
    sub-sequence :- Need not to be contiguous but maintains order.
    subset: Same as subsequence except it has empty set.
    */
    static class Solver {
        public int solve1(int[] arr) {
            return 0;
        }
    }
}
